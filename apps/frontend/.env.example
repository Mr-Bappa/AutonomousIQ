# Base URL of the FastAPI backend. In docker-compose this is overridden
# to http://localhost:8000 (the browser calls out of the container, so
# it must be a host-reachable URL, not the internal "api" service name).
VITE_API_BASE_URL=http://localhost:8000
